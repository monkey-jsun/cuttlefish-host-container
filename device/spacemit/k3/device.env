# Sourced by cf-build.sh --device spacemit/k3.
# Default image name for this device build (override with cf-build.sh -i).
# The PowerVR DDK version pin lives as ARG PVR_VERSION in the Dockerfile and
# must be bumped together with the booted kernel -- see the Dockerfile comment.
IMAGE=cf-host-spacemit-k3
